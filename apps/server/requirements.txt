# PDF OCR 依赖
# 安装命令: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 核心OCR库 (RapidOCR - 轻量级ONNX推理)
rapidocr-onnxruntime>=1.3.0

# PDF处理
pdf2image>=1.16.0
pdfplumber>=0.9.0
pypdf>=4.0.0

# Excel处理
openpyxl>=3.1.0

# 图像处理
pillow>=9.0.0

# 工具库
numpy>=1.21.0
requests>=2.28.0       # HTTP请求
psutil>=5.9.0          # 系统资源监控

# Windows 打印支持
pywin32>=306; sys_platform == 'win32'

# 测试工具
pytest>=8.0.0
pytest-cov>=4.0.0      # 测试覆盖率
pytest-asyncio>=0.21.0 # 异步测试支持

# ==================== 可选：本地LLM支持 ====================
# 取消下面注释以启用本地AI模型功能

# llama-cpp-python 用于运行 GGUF 格式模型（推荐）
# 安装命令（CPU版本）:
#   pip install llama-cpp-python --no-cache-dir
# 安装命令（支持GPU/CUDA）:
#   CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python --no-cache-dir

# transformers + torch 用于运行 HuggingFace 格式模型
# transformers>=4.36.0
# torch>=2.0.0

# 其他LLM工具
# sentencepiece>=0.1.99  # 部分模型需要
# protobuf>=3.20.0       # 部分模型需要
